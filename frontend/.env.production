# Production environment variables
# These will be baked into the build

# Relative path — Nginx proxies /api to the backend internally.
# Works for local (http://10.0.0.231) and external (https://diabetes1.diegocastilho.me).
VITE_API_URL=/api
VITE_WS_URL=ws://10.0.0.229:3001
