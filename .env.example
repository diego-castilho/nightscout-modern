# ============================================================================
# Nightscout Modern - Environment Variables
# ============================================================================
# 1. Copy this file:  cp .env.example .env
# 2. Fill in your values below
# 3. Run: docker compose up -d
# ============================================================================

# Timezone (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=America/Sao_Paulo

# ----------------------------------------------------------------------------
# MongoDB
# Point to your existing Nightscout database.
# Examples:
#   Local:       mongodb://localhost:27017
#   Remote:      mongodb://192.168.1.10:27017
#   Atlas:       mongodb+srv://user:pass@cluster.mongodb.net
#   With auth:   mongodb://user:pass@host:27017
# ----------------------------------------------------------------------------
MONGODB_URI=mongodb://YOUR_MONGO_HOST:27017
MONGODB_DB_NAME=nightscout   # common values: "nightscout", "test", "cgm"
MONGODB_USER=                # leave empty if no auth
MONGODB_PASSWORD=            # leave empty if no auth

# ----------------------------------------------------------------------------
# Security
# Use your existing Nightscout API_SECRET, or generate a new one.
# JWT_SECRET: generate with: openssl rand -hex 32
# ----------------------------------------------------------------------------
NIGHTSCOUT_API_SECRET=your-nightscout-api-secret
NIGHTSCOUT_MODERN_JWT_SECRET=your-random-jwt-secret-min-32-chars

# ----------------------------------------------------------------------------
# CORS — origins allowed to call the API (comma-separated).
# Must include every URL where the frontend will be accessed from.
# Examples:
#   Local only:          http://localhost
#   Local + public URL:  http://localhost,https://cgm.example.com
# ----------------------------------------------------------------------------
CORS_ORIGIN=http://localhost

# ----------------------------------------------------------------------------
# Frontend port (default: 80)
# The port on the Docker host that serves the web app.
# Example: FRONTEND_PORT=8080 → access at http://localhost:8080
# ----------------------------------------------------------------------------
FRONTEND_PORT=80

# ----------------------------------------------------------------------------
# LibreLink MCP Integration (optional)
# Leave empty to disable. Pulls glucose readings directly from LibreLink Cloud.
# ----------------------------------------------------------------------------
LIBRELINK_USERNAME=
LIBRELINK_PASSWORD=
LIBRELINK_REGION=LA   # LA | US | EU | DE | FR | JP | AU

# ----------------------------------------------------------------------------
# Log level (optional, default: info)
# Options: error | warn | info | debug
# ----------------------------------------------------------------------------
LOG_LEVEL=info

# ----------------------------------------------------------------------------
# MacVLAN networking — only needed with docker-compose.macvlan.yml override
# See README.md for MacVLAN setup instructions.
# ----------------------------------------------------------------------------
# BACKEND_IP=10.0.0.X
# FRONTEND_IP=10.0.0.Y
# MACVLAN_DNS=10.0.0.1
